import { AppLayoutAdmin } from "components/layouts/AppLayoutAdmin";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <AppLayoutAdmin>
      Catégories
    </AppLayoutAdmin>
  );
};

export default Home;
