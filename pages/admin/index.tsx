import { AppLayoutAdmin } from "components/layouts/AppLayoutAdmin";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <AppLayoutAdmin>
      DashBoard
    </AppLayoutAdmin>
  );
};

export default Home;
